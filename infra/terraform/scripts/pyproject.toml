[project]
name = "agent-deploy-scripts"
version = "0.1.0"
description = "Deployment scripts for AI Agent Code Modernizer"
requires-python = ">=3.10"

dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["."]
